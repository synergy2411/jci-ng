import { UntilDirective } from './until.directive';

describe('UntilDirective', () => {
  it('should create an instance', () => {
    const directive = new UntilDirective();
    expect(directive).toBeTruthy();
  });
});
